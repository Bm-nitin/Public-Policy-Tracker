# Copy this file to .env and fill in real values. .env itself is gitignored
# and must never be committed.

# --- Required for AI fallback (chatbot works without it, with reduced
# functionality - see backend/config.py) ---
GEMINI_API_KEY=

# --- Optional overrides (safe to leave unset - defaults shown) ---
# GEMINI_MODEL=gemini-2.5-flash
# CORS_ORIGINS=*
# HOST=0.0.0.0
# PORT=5000

# --- Database (Phase 2 - optional; app runs fine with this unset) ---
# postgresql+psycopg selects the psycopg3 driver installed via
# requirements.txt. Replace with real credentials in your local .env only
# - never commit real values.
# DATABASE_URL=postgresql+psycopg://username:password@localhost:5432/policy_tracker

# --- Email (Phase 4 - optional; registration works without it, but
# verification emails won't send until these are configured) ---
# MAIL_HOST=smtp.example.com
# MAIL_PORT=587
# MAIL_USERNAME=
# MAIL_PASSWORD=
# MAIL_FROM=no-reply@your-domain.example
# MAIL_USE_TLS=true

# --- Frontend (Phase 4 - used to build verification links) ---
# FRONTEND_URL=https://your-frontend.example

# --- Sessions / Auth (Phase 5) ---
# SECRET_KEY=  # set a long random value in production; a random ephemeral
#              # one is generated per-process if left unset (see backend/config.py)
# SESSION_COOKIE_NAME=ppt_session
# SESSION_LIFETIME_HOURS=168
# Defaults below (None + true) are correct for the real cross-origin
# frontend/backend deployment. For local plain-HTTP development where
# frontend and backend share an origin/site, override BOTH together:
# SESSION_COOKIE_SAMESITE=Lax
# SESSION_COOKIE_SECURE=false

# --- Password reset (Phase 6) ---
# PASSWORD_RESET_TOKEN_EXPIRY_MINUTES=30
# PASSWORD_RESET_RESEND_COOLDOWN_SECONDS=60
